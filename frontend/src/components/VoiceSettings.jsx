import React from 'react';

// Voice settings are now handled automatically by the backend (Multilingual Voice)
const VoiceSettings = ({ isOpen, onClose }) => {
    if (!isOpen) return null;
    return null;
};

export default VoiceSettings;
